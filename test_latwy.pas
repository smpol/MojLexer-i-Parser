program ProgramZeStalymi;

const
  PI = 3.14159;
  GRAVITY = 9.8;
  MAX_VALUE = 100;
  napis = 'string';

var
  radius: array [2..5] of integer;
  area: real; 
  force: real;
  value: integer;

function funkcja (n:string):integer; 
      begin
            write(test);
      end

function funk2cja (n:integer; s:string):integer;
      const
            a=20;
	      var
            a, i, j, k:integer;
      begin
	write('siema');
      end


function dodaj(a,b: integer; c: real): real;
begin
write('ss');
end

begin
	write('silnia');
	a:=20;
	b:='napis';
	if a > 0 and b >= 0 or c = 2 or b < 2 and d <= 10 or not d then
		begin
				writeln('if');
		end
	else 
  	begin
    		writeln('Podana liczba jest ujemna.');
  	end;
	
	write('siema');
	writeln('siema2');
	read(a);
	readln(b);
  	while a > 0 do
	begin
		a := a-1;
	end

	repeat
    		writeln('siema');
    		liczba := liczba + 1;
  	until liczba > 10;

	for i:= 0 to a do
	begin
		read(a);
	end

	for i:= 10 downto 0 do
	begin
		read(a);
	end
end.

